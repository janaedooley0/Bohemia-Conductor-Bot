# Telegram Bot Configuration
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# JotForm API Configuration
# Get your API key from https://www.jotform.com/myaccount/api
JOTFORM_API_KEY=your_jotform_api_key_here

# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Cache Configuration (optional)
# Time-to-live for cached data in seconds (default: 300 = 5 minutes)
CACHE_TTL_SECONDS=300

# Database Configuration (optional)
# The bot uses SQLite for persistent storage. The database file (bot_data.db)
# is automatically created in the bot/ directory on first run.
# Data stored: current GB setting, admin list, analytics

# Admin Contact Configuration (for problem reports)
# The Telegram username of the admin who should receive problem reports
ADMIN_USERNAME=Emilycarolinemarch

# Optional: Admin's Telegram Chat ID for direct message forwarding
# To get this: have the admin send a message to the bot, then check the logs
# for their chat ID, or use @userinfobot on Telegram
# ADMIN_CHAT_ID=123456789

# Conversation Timeout (optional)
# How long (in seconds) before multi-step conversations expire (default: 300 = 5 minutes)
CONVERSATION_TIMEOUT_SECONDS=300

# OpenAI Configuration (optional - defaults shown)
# OPENAI_TIMEOUT_SECONDS=30
# OPENAI_MAX_RETRIES=3
# OPENAI_BACKOFF_SECONDS=1

# JotForm Configuration (optional - defaults shown)
# JOTFORM_MAX_RETRIES=3
# JOTFORM_BACKOFF_SECONDS=1
